# @gpro/ui

Framework-agnostic UI primitives and view-model builders (sections, tables, bars) shared between the Tampermonkey panel and any future web app, so UI is not duplicated per surface.

**Status:** scaffold only — not yet wired into the live userscript. See [ARCHITECTURE.md](../../ARCHITECTURE.md) for the migration plan and current TODOs.
