# @gpro/calculations

Pure, deterministic calculation engines: tyre strategy, car setup, parts wear, fuel, PHA. Framework-agnostic; the userscript and any future web app both call into this.

**Status:** scaffold only — not yet wired into the live userscript. See [ARCHITECTURE.md](../../ARCHITECTURE.md) for the migration plan and current TODOs.
