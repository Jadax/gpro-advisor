# @gpro/api

GPRO API client: auth, endpoint definitions, request budget enforcement, retry policy. Platform-agnostic (works via GM_xmlhttpRequest in the userscript or fetch in a web app).

**Status:** scaffold only — not yet wired into the live userscript. See [ARCHITECTURE.md](../../ARCHITECTURE.md) for the migration plan and current TODOs.
